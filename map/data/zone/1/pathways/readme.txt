Contiene per ciascun trip:
- il file geoJson con la polyline 2.5d (cioè con l'altimetria) 
- il file geoJson con i waypoint (cioè i trackpoints)
- il file csv con i roadbook elements 
- il file csv con la descrizione trip   

tutti i file usano il codice del trip

(solo offline)

